apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    acornsoft.io/workload-group-no: "1"
  generation: 1
  labels:
    app: commerce-order
  name: commerce-order
  namespace: bb-edu
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: commerce-order
      version: v1
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        app: commerce-auth
        version: v1
    spec:
      containers:
      - image: 192.168.100.12/bb-edu/commerce-order:v1.0.1
        imagePullPolicy: Always
        name: order-cotainer
      dnsPolicy: ClusterFirst
      imagePullSecrets:
      - name: commerce-cocktail-t8ceb0dry5v7
